<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="{{ path('home') }}">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a href="{{ path('idea_list') }}" class="nav-link btn btn-light">Idea's list</a>
            </li>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item active">
                    <a class="nav-link btn btn-light" href="{{ path('idea_add') }}">Add an idea</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link btn btn-light" href="{{ path('user_logout') }}">Disconnect</a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link btn btn-light" href="{{ path('user_login') }}">Sign in / Register</a>
                </li>
            {% endif %}
            <li class="nav-item active">
                <a class="nav-link btn btn-light" href="{{ path('aboutus') }}">About us</a>
            </li>
        </ul>
    </div>
</nav>